#include "phonebook.hpp"
#include <string.h>
#include <iostream>

bool  PhoneBook::askStringInput(const std::string& ask, std::string& result) {

}

void PhoneBook::add() {
  std::string input;

  while (!askStringInput());
  while (!askStringInput());
  while (!askStringInput());
  while (!askStringInput());
  while (!askStringInput());
}

void PhoneBook::search() {
}

void PhoneBook::exit() {
}
